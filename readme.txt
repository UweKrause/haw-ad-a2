Zum Benutzen müssen alle Module kompiliert werden.

Hier eine Liste:

c(ssort).
c(ksort).
c(liste_util).
c(util).
c(wechsel).
c(laufzeit).



ACHTUNG:
1. Ihre UTIL.erl wurde agepasst und nutzt statt "rand" jetzt "random"!!
Sollte das bei Ihnen nicht funktionieren, bitte Ihre eigenen util benutzen.


2. In der Vorlesung sagten Sie, dass es okay sei, Eingebaute Funktionen (beispielsweise von lists) zu verwenden, sofern wir beweisen können, dass sie genau das tun, was wir wollen.
An einigen Stelen (hauptsächlich in der liste_util.erl) habe ich mich darauf bezogen und erst einmal aus Zeitmangel teilweise die Eingebauten Funktionen benutzt.
Der Aufbau des Codes würde mir eine Nacharbeitung erlauben, die komplett auf selbstgeschriebenen Funktinen basiert, wenn mehr Zeit wäre.


3. Testdaten
Erstellen der Testdaten nach  Laden der laufzeit.erl
(Achtung, results.csv muss vorher erstellt und eine leere Datei sein.)
(Wenn sie nicht leer ist, werden die Ergebnise hinten angehangen!)

Aufruf der Testmethode durch laufzeit:log_to_file().



3. Wechselgrösse Quicksort
Erstellen der Testdaten nach Laden der wechsel.erl
(Achtung, wechsel.csv muss vorher erstellt und eine leere Datei sein.)
(Wenn sie nicht leer ist, werden die Ergebnise hinten angehangen!)

Aufruf der Testmethode durch wechsel:log_to_file().